{
  "name": "mdtxtrt",
  "version": "1.0.0",
  "description": "Editor Markdown otimizado para Telegram Rich Messages (Mini App)",
  "scripts": {
    "start": "npx serve -s . -l tcp://0.0.0.0:$PORT"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
