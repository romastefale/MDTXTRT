# Arquivo

Código e testes que **não** entram em `python app.py`.

A UI de produção é `mdtxtrt/static`. Este diretório guarda o runtime legado (`main.py`, `runtime_v2.py`, `install()`, `ui.N.js`) e os testes que o importavam.

Não adicionar `archive/` ao `PYTHONPATH`.
